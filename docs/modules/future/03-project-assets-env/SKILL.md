# 模块技能：项目、资产与环境变量

## 0. 强制执行约束（必须遵守）

- 开发前先读取仓库根 `AGENTS.md` 与 `docs/project/project-progress.md`
- 本模块关联阶段为阶段 2；当前总阶段为阶段 2，允许执行实现类开发
- 如用户明确要求越阶段落地实现，可执行，但必须在 `project-progress.md` 中记录风险与越阶段原因
- 涉及逻辑或功能代码改动时，必须补充/更新测试并执行最小相关测试集；影响公共模块时补充回归测试
- 改动影响功能实现、模块状态、架构分层或路线图时，必须同步更新 `docs/project/project-progress.md`，并检查 `docs/architecture/企业级自动化测试平台系统架构规划.md`
- 当本阶段或其他阶段状态发生变化时，必须同步更新对应阶段目录下的 `SKILL.md`，不得只更新总进度文档

## 1. 模块目标

建立统一的测试资产管理能力，支撑项目、环境、变量、密钥等核心资源的治理与复用。

## 2. 当前状态

- 阶段状态：进行中（阶段 2）
- 关联阶段：阶段 2
- 风险等级：中高
- 已完成项：环境与变量核心模型落地（`project_environments`、`project_variables`、`environment_variables`）
- 已完成项：环境与变量管理接口落地（`/api/environments/*`）
- 已完成项：敏感变量默认脱敏展示
- 已完成项：变量优先级解析与执行链路替换接入
- 已完成项：变量组复用（项目变量分组 + 环境绑定）与运行时解析接入
- 已完成项：密钥受控读取能力（仅管理权限可明文读取，审计留痕）
- 已完成项：前端环境变量治理页与执行详情变量快照联动
- 待完成项：第三方密钥管理系统深度对接（企业集成阶段）

## 3. 范围边界

### In Scope
- 项目资源模型增强
- 环境管理（dev/test/staging/prod）
- 变量管理（全局/项目/环境）
- 密钥管理与脱敏展示
- 标签、分组、检索基础能力

### Out of Scope
- 第三方密钥管理系统深度对接（后续）

## 4. 关键任务

1. 持续增强变量组治理能力（版本化、冲突检测、批量编排）
2. 持续增强变量解析与优先级可观测性
3. 评估并设计第三方密钥管理系统对接方案
4. 持续增强前端治理页操作体验与结果追踪可视化
5. 持续增强执行链路中的变量替换与审计能力

## 5. 交付物

- 环境变量模型文档
- 环境/变量/密钥管理接口
- 变量替换引擎实现
- 管理页面与操作日志

## 6. 测试要求

- 变量覆盖优先级测试
- 密钥脱敏与权限测试
- 环境切换执行正确性测试
- 边界值与缺失变量错误测试

## 7. 完成定义（DoD）

- API 与 Web 执行都可复用统一环境变量体系
- 变量解析行为可预测且可验证
- 核心环境管理流程测试通过
- 关键变量治理能力（变量组/密钥托管）具备可验证测试
