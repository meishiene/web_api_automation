# 企业级自动化测试平台系统架构规划

## 1. 文档目的

本文档用于定义当前项目从 **MVP 级 API 自动化测试工具** 演进为 **企业级自动化测试平台** 的整体方向，作为后续架构设计、模块拆分、迭代排期与开发实施的总纲。

本文档重点回答以下问题：
- 当前系统处于什么阶段
- 企业级目标平台应该具备哪些能力
- API 测试与 Web 测试如何统一纳入平台能力体系
- 接下来每个阶段应该优先做什么
- 如何以最小风险从当前代码逐步演进到企业级平台

## 2. 当前系统现状

### 2.1 当前已具备能力
- 用户注册、登录、刷新令牌
- 项目管理（含成员管理）
- 组织管理与跨项目成员治理基础能力
- API 测试用例管理（含断言规则/提取规则）
- API 套件管理与有序编排
- 单条与套件批量执行（含批次追踪）
- 环境与变量管理（项目级/环境级，敏感变量脱敏）
- 执行结果记录、批次明细查询与基础展示
- 前后端联通主链路（登录/项目/用例/执行）

### 2.2 当前技术形态
- 后端：`FastAPI + SQLAlchemy + SQLite + httpx`
- 前端：`Vue 3 + Vite + Vue Router + axios`
- 架构模式：前后端分离、单体应用、本地数据库持久化
- 认证方式：JWT Bearer + Refresh Token（最小 RBAC + 权限矩阵 + 资源级鉴权基础已落地）

### 2.3 当前定位判断
当前系统属于：

- **阶段定位：阶段 2 启动期（阶段 1 暂停）**
- **能力重心：从单用例执行迈向 API 平台化（套件、批量、环境变量）**
- **成熟度：MVP 向平台化过渡期，尚不具备企业级完整能力**

### 2.4 当前进度评估

从企业级平台视角评估当前进度如下：

| 领域 | 当前状态 | 进度评估 |
| --- | --- | --- |
| 用户与认证 | 已完成 JWT + Refresh Token、最小 RBAC（admin/user）、角色-权限矩阵、项目成员与组织层权限基础能力 | 80% |
| 项目管理 | 已有基本 CRUD、项目成员管理与组织归属治理基础能力 | 60% |
| API 用例管理 | 已支持基础 CRUD、套件关联与增强断言配置（阶段 2 首批已落地） | 65% |
| API 执行能力 | 已支持单条执行与套件批量执行（含批次追踪、变量链路传递） | 55% |
| 工程化与测试基线 | 已建立测试基线、统一异常错误码、结构化日志、审计治理闭环、Alembic/PG 本地测试落地与模型治理细则 | 90% |
| Web 测试能力 | 未开始 | 0% |
| 环境与变量管理 | 已落地项目/环境变量最小闭环（含脱敏展示） | 35% |
| 套件与批量执行 | 已落地 API 套件与批量执行首批闭环 | 45% |
| 调度与队列 | 仅有预留模型 | 5% |
| 报告与分析 | 仅有基础结果记录 | 5% |
| 权限与治理 | 已完成最小 RBAC 闭环并推进细粒度治理（权限矩阵、越权校验、项目成员协作、组织层与跨项目治理基础） | 55% |
| 企业集成 | 未开始 | 0% |

### 2.5 当前阶段结论
- 当前项目适合作为企业级平台的种子版本继续演进
- 当前代码可以保留，不建议推倒重来
- 阶段 1 已按决策暂停，阶段 2 已启动并进入 API 平台化建设期
- 后续重点转向 API 平台化能力落地，同时托管阶段 1 遗留风险

## 3. 平台愿景与建设目标

## 3.1 平台愿景
建设一个统一的企业级自动化测试平台，支持 **API 测试** 与 **Web 测试** 两大核心能力，并围绕测试资产管理、执行调度、报告分析、权限治理和企业集成形成完整闭环。

## 3.2 目标平台能力范围

平台中长期目标应覆盖以下能力：
- 统一用户、组织、权限体系
- 统一项目、环境、变量、密钥管理
- API 自动化测试设计、执行与回归
- Web 自动化测试设计、执行与回归
- 套件、批次、计划、调度、队列、Worker 分发
- 统一执行报告、趋势分析、稳定性分析
- 与 CI/CD、消息通知、SSO、缺陷系统集成

## 3.3 平台建设原则
- **先平台底座，后功能扩张**
- **先统一模型，后增加模块**
- **API 与 Web 共用治理体系**
- **先单体清晰分层，再考虑服务拆分**
- **所有执行必须可追踪、可审计、可分析**
- **优先保证可演进性，不追求短期功能堆砌**

## 4. 企业级平台总体能力蓝图

建议将平台拆分为六层能力结构。

### 4.1 接入层
- Web 管理端
- 开放 API
- CI/CD 触发入口
- Webhook 入口
- 消息通知入口

### 4.2 业务平台层
- 用户与组织中心
- 项目与资产中心
- API 测试中心
- Web 测试中心
- 执行与调度中心
- 报告与分析中心
- 系统管理中心

### 4.3 测试引擎层
- API 执行引擎
- Web 执行引擎
- 断言引擎
- 变量解析与数据提取引擎
- 报告产物生成引擎

### 4.4 调度分发层
- 调度器 `Scheduler`
- 任务队列 `Queue`
- Worker 执行节点
- 重试、取消、超时控制
- 并发与资源限制控制

### 4.5 数据与存储层
- 主数据库
- 缓存
- 对象存储
- 日志存储
- 指标存储

### 4.6 基础设施层
- 认证鉴权
- 配置中心
- 监控告警
- 审计日志
- 安全与密钥管理

## 5. 统一业务域规划

为了支撑长期演进，建议按领域划分，而不是继续以页面或单文件接口为中心扩展。

### 5.1 用户与组织域
核心对象：
- `User`
- `Role`
- `Permission`
- `Workspace`
- `Department`
- `ProjectMember`
- `AuditLog`

职责：
- 用户认证与身份识别
- 项目成员管理
- 角色权限控制
- 操作审计与合规支持

### 5.2 项目与资产域
核心对象：
- `Project`
- `Service`
- `Environment`
- `Variable`
- `Secret`
- `Tag`

职责：
- 测试资产归属管理
- 环境隔离
- 配置与变量复用
- 项目级资源治理

### 5.3 API 测试域
核心对象：
- `ApiTestCase`
- `ApiTestSuite`
- `ApiAssertion`
- `ApiDataBinding`
- `ApiMockConfig`（后续可选）

职责：
- API 用例设计
- 请求配置管理
- 断言与数据提取
- 套件编排与批量执行

### 5.4 Web 测试域
核心对象：
- `WebTestCase`
- `WebTestSuite`
- `PageObject`
- `Locator`
- `WebStep`
- `BrowserProfile`

职责：
- Web UI 自动化测试设计
- 页面对象与元素定位管理
- 步骤编排与浏览器执行
- 截图、视频、Trace 等产物归档

### 5.5 执行域
核心对象：
- `ExecutionTask`
- `ExecutionJob`
- `ExecutionStep`
- `ExecutionResult`
- `Artifact`

职责：
- 统一承载 API 与 Web 执行过程
- 抽象手工执行、批量执行、计划执行等任务模型
- 形成统一执行结果中心

### 5.6 调度与回归域
核心对象：
- `ScheduleTask`
- `RunQueue`
- `TestPlan`
- `TriggerRule`

职责：
- 定时执行
- 队列分发
- 回归计划管理
- 优先级、重试、超时等任务控制

### 5.7 报告与分析域
核心对象：
- `ExecutionReport`
- `TrendMetric`
- `FailureAnalysis`
- `QualityDashboard`

职责：
- 汇总执行结果
- 生成报告和趋势视图
- 识别失败模式和波动风险

## 6. API 与 Web 测试统一架构设计

## 6.1 统一原则
API 测试和 Web 测试不应建设为两个完全独立的平台，而应复用一套统一底座：
- 统一项目
- 统一环境
- 统一变量
- 统一权限
- 统一调度
- 统一结果中心
- 统一报告体系

## 6.2 API 测试能力清单
- 请求方法、路径、Header、Body、Query、Path 参数管理
- 状态码断言
- 响应体断言
- JSONPath / JMESPath / Schema 校验
- 变量提取与链路传递
- 套件执行、批量执行、回归计划执行

## 6.3 Web 测试能力清单
- 页面访问
- 元素定位与操作
- 等待与同步机制
- 文本、属性、状态断言
- 页面跳转与流程校验
- 截图、视频、Trace 产物输出
- 多浏览器执行
- UI 回归报告展示

## 6.4 Web 测试引擎建议
Web 测试执行引擎建议优先采用 `Playwright`，原因如下：
- 浏览器自动化能力成熟
- 原生支持截图、视频、Trace
- 支持 Chromium、Firefox、WebKit
- 对现代前端页面兼容性更好
- 更适合作为企业级 UI 自动化测试底座

平台架构层应保留执行引擎抽象，不把实现永久绑定到单一技术上，未来可兼容 Selenium 或其他执行器。

## 7. 目标系统架构规划

## 7.1 近期架构形态
短期建议仍维持 **单体应用 + 清晰分层 + 模块化领域边界**，避免过早拆分微服务。

### 近期建议结构
- 前端平台：统一工作台与业务模块页面
- 后端单体：按领域分模块组织
- 数据层：单主库
- 执行层：先单机执行，再引入独立 Worker

## 7.2 中期架构形态
当 API 与 Web 能力完成平台化后，逐步引入：
- 调度器
- 队列系统
- 独立 Worker 节点
- 对象存储
- Redis 缓存

## 7.3 长期架构形态
长期可演进为分层平台架构：

- `gateway / api layer`
- `auth service`
- `project service`
- `test asset service`
- `execution service`
- `schedule service`
- `report service`
- `notification service`

此阶段是否拆分为独立服务，应基于团队规模、并发规模和运维能力决定，不建议在项目早期强行微服务化。

## 8. 技术演进建议

## 8.1 数据库
建议由 `SQLite` 迁移到企业级数据库：
- 推荐：`PostgreSQL`
- 可选：`MySQL`

原因：
- 更适合并发访问
- 支持更完整的数据管理能力
- 更利于企业环境部署与备份恢复

## 8.2 缓存与任务中间件
建议引入：
- `Redis`：缓存、分布式锁、轻量任务状态保存
- 队列机制：初期可基于数据库或 Redis，后期视规模演进为消息队列

## 8.3 Web 测试执行能力
- 推荐执行引擎：`Playwright`
- 需要配套：浏览器执行环境、产物目录管理、结果上传机制

## 8.4 工程化能力
必须逐步补齐：
- 数据库迁移工具
- 统一异常处理
- 统一错误码规范
- 结构化日志
- 自动化测试体系
- 配置分环境管理

## 9. 分阶段开发路线图

以下阶段路线用于作为后续开发总纲。

## 9.1 阶段 0：MVP 雏形阶段

### 阶段状态
- **当前状态：已完成**

### 已完成事项
- 用户注册登录最小闭环
- 项目管理最小闭环
- API 用例管理最小闭环
- 单条 API 用例执行
- 执行结果记录

### 阶段结论
已经完成从 0 到 1 的最小验证，但尚不具备平台化建设条件。

## 9.2 阶段 1：平台基础重构阶段

### 阶段状态
- **当前状态：暂停**

### 阶段目标
把当前 MVP 改造为可持续演进的平台底座。

### 已完成关键任务
- 认证机制从 `X-User-ID` 升级为 `JWT + Refresh Token + Bearer`
- 密码哈希存储改造（含历史明文兼容迁移）
- 引入数据库迁移工具（Alembic）并完成初始 schema 基线
- PostgreSQL 本地/测试环境落地（配置切换 + compose + 一键脚本）
- 统一异常体系与错误码规范
- 建立结构化日志与请求 `request_id` 追踪
- 建立审计日志模型与关键写操作审计落库
- 建立后端测试基线并持续回归
- 路由层 DTO 已统一收敛到 `app/schemas/`
- 核心 ORM 模型已补齐 `relationship + back_populates` 关系映射
- 完成领域模型治理细则落地（约束、索引、级联、生命周期）
- 已提供生产环境迁移脚本（不含压测）与执行文档
- 完成审计治理闭环（查询接口、归档表、保留策略脚本）
- 完成最小 RBAC/权限校验闭环（`users.role` + 权限依赖 + admin 治理接口）

### 剩余关键任务
- PostgreSQL 真实生产环境迁移窗口执行演练（本地实操演练与留档已完成，按当前决策不做压测）
- 认证治理收口（组织层策略深化、跨项目治理增强与更完整权限矩阵）
- 已固化迁移发布与回滚流程规范，待在真实生产窗口完成演练闭环
- 审计治理定时任务与告警平台联动生产验证（Task Scheduler 与 Webhook 脚本已落地，待生产环境注册与联调）
- 注：本阶段已按项目决策暂停，剩余任务转为风险托管项。

### 本阶段完成标志
- 平台具备规范的基础设施能力
- 后续业务扩展不再依赖临时性实现

### 预计总进度
- 阶段完成后平台可稳定进入阶段 2（API 平台化）

## 9.3 阶段 2：API 测试平台化阶段

### 阶段状态
- **当前状态：进行中**

### 阶段目标
把当前 API 测试能力从“单用例执行工具”升级为“团队可用的回归平台”。

当前进展：阶段 2 首批后端能力已落地（套件、批量执行、环境变量、断言增强）。

### 已完成关键任务（首批）
- 套件模型与接口：`api_test_suites`、`api_test_suite_cases`、`/api/test-suites/*`
- 套件批量执行与批次查询：`/api/test-runs/suites/{suite_id}/run`、`/api/test-runs/batches/*`
- 环境与变量模型及接口：`project_environments`、`project_variables`、`environment_variables`、`/api/environments/*`
- 执行引擎增强：变量替换、`contains/regex/jsonpath` 断言、响应提取链路

### 剩余关键任务
- 持续优化 API 用例治理能力与检索性能
- 增加用例复制、导入、导出能力
- 增强断言能力：持续完善复杂断言规则与错误定位能力
- 持续增强失败重试策略与幂等保护
- 持续增强执行详情页与批次结果页展示维度（基础页面链路已落地）

### 本阶段完成标志
- API 测试具备企业团队使用的基础能力
- 能支撑真实项目的回归执行

### 预计总进度
- 完成后平台整体进度约为 **45%**

## 9.4 阶段 3：Web 测试平台建设阶段

### 阶段状态
- **当前状态：未开始**

### 阶段目标
建立 Web 自动化测试模块，使平台具备 API + Web 双引擎能力。

### 关键任务
- 建立 `WebTestCase`、`WebTestSuite`、`PageObject`、`Locator`、`WebStep` 等领域模型
- 接入 `Playwright` 执行引擎
- 支持常见 Web 操作步骤：打开页面、点击、输入、等待、断言、截图
- 建立浏览器执行配置与超时控制机制
- 支持执行产物管理：截图、视频、Trace
- 提供 Web 用例管理页面与执行结果页
- 支持 API 与 Web 结果统一归档、统一展示

### 本阶段完成标志
- 平台从 API 自动化工具升级为综合自动化测试平台
- Web 测试具备初步回归能力

### 预计总进度
- 完成后平台整体进度约为 **60%**

## 9.5 阶段 4：调度与分布式执行阶段

### 阶段状态
- **当前状态：未开始**

### 阶段目标
把平台从手工触发执行升级为可计划、可分发、可扩展的企业回归执行平台。

### 关键任务
- 完善 `schedule_tasks` 业务能力
- 完善 `run_queue` 业务能力
- 支持 Cron、定频、指定时间等调度方式
- 建立任务优先级、重试、取消、超时处理机制
- 建立 Worker 执行节点体系
- 支持 API Worker 与 Web Worker 分工执行
- 支持并发控制、资源限制、任务追踪
- 建立任务与产物的统一追踪链路

### 本阶段完成标志
- 支持定时回归、批量回归、多项目并发执行
- 平台具备企业执行中台雏形

### 预计总进度
- 完成后平台整体进度约为 **75%**

## 9.6 阶段 5：报告分析与治理阶段

### 阶段状态
- **当前状态：未开始**

### 阶段目标
把平台从执行系统升级为质量分析与治理系统。

### 关键任务
- 建立执行报告中心
- 建立套件报告、回归报告、版本对比报告
- 增加通过率、失败率、稳定性趋势分析
- 增加失败聚类、慢步骤分析、问题排行
- 建立项目级、服务级、团队级质量看板
- 增加操作审计、资源治理、数据保留策略

### 本阶段完成标志
- 平台能够支撑测试质量分析、趋势决策和治理落地

### 预计总进度
- 完成后平台整体进度约为 **88%**

## 9.7 阶段 6：企业集成与生态完善阶段

### 阶段状态
- **当前状态：未开始**

### 阶段目标
实现企业生态接入，使平台可在真实企业环境中广泛落地。

### 关键任务
- 接入 Jenkins、GitHub Actions、GitLab CI 等 CI/CD 平台
- 建立 Webhook 和流水线触发机制
- 支持 SSO、LDAP、OAuth2 等企业认证方式
- 对接飞书、钉钉、企业微信、邮件通知
- 对接 Jira、禅道等缺陷管理平台
- 支持 OpenAPI / Swagger / Postman Collection 导入
- 预留插件化扩展机制

### 本阶段完成标志
- 平台达到企业级正式推广使用条件

### 预计总进度
- 完成后平台整体进度约为 **100%**

## 10. 当前版本到目标版本的实施优先级

建议按以下优先级推进：

### P0：阶段 2 API 平台化优先（剩余项）
- 断言能力与失败定位能力持续增强
- 失败重试与幂等保护持续增强
- API 用例治理能力持续优化
- 用例复制、导入、导出

### P1：阶段 1 遗留风险托管
- PostgreSQL 真实生产窗口迁移演练
- 审计治理定时任务与告警平台生产联调
- 组织层策略深化收口

### P2：Web 模块落地
- Web 模型
- Playwright 引擎
- Web 执行页面
- 统一执行结果

### P3：调度与 Worker
- Scheduler
- Queue
- Worker
- 并发控制

### P4：治理与企业集成
- 报告分析
- 审计治理
- CI/CD
- SSO
- 缺陷平台联动

## 11. 与当前代码的衔接建议

当前代码中已经存在一些可直接作为未来建设起点的部分：

- `app/services/test_executor.py`
  - 可演进为 `API Runner`
- `app/models/schedule_task.py`
  - 可作为调度系统模型起点
- `app/models/run_queue.py`
  - 可作为队列系统模型起点
- `app/api/test_runs.py`
  - 可作为统一执行入口的早期形态
- `frontend/src/views/TestCaseList.vue`
  - 后续应拆分为用例管理页、执行详情页、结果页

需要特别注意的是：
- 当前认证方案只能作为过渡实现，不能继续扩展为正式企业方案
- 当前 SQLite 仅适合开发验证，不适合作为企业级持久化基础
- 当前前端页面职责较重，后续必须模块化拆分

## 12. 里程碑建议

建议把后续开发分成以下里程碑：

### M1：平台底座完成
- 完成阶段 1

### M2：API 平台化完成
- 完成阶段 2

### M3：Web 测试能力上线
- 完成阶段 3

### M4：调度与 Worker 上线
- 完成阶段 4

### M5：报告治理能力上线
- 完成阶段 5

### M6：企业集成上线
- 完成阶段 6

## 13. 风险与注意事项

### 13.1 架构风险
- 过早追求微服务会显著增加复杂度
- 在领域模型未稳定前，不宜大规模拆服务

### 13.2 产品风险
- 如果未先建设统一环境、变量、执行模型，后续 API 与 Web 能力会出现重复建设
- 如果只做执行，不做结果治理，平台价值会停留在工具层面

### 13.3 实施风险
- Web 测试执行环境对资源消耗更高，需要提前规划 Worker 和执行机管理
- 报告、截图、视频、Trace 会带来较大的存储压力，需要尽早设计产物存储策略

## 14. 结论

当前项目已经完成了企业级自动化测试平台的最小种子版本，但仍处于非常早期的 MVP 阶段。

后续开发应遵循以下主线：
- 先推进阶段 2 API 平台化能力落地
- 并行托管阶段 1 遗留风险项
- 然后建设 Web 测试模块
- 再推进调度、队列、Worker
- 最后补齐报告分析、治理能力和企业生态集成

从当前阶段看，最佳策略不是推翻重写，而是基于现有闭环能力逐步演进，最终形成一个统一承载 **API 测试 + Web 测试 + 调度执行 + 报告治理 + 企业集成** 的企业级自动化测试平台。
