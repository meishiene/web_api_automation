# 阶段 5 验收清单（S5 Acceptance Checklist）

## 1. 验收范围
- 验收阶段：阶段 5（报告分析与治理）
- 验收口径：以 `app/`、`frontend/src/`、`tests/backend/` 当前代码事实与测试结果为准
- 当前阶段状态：S5-00~S5-07 已完成，阶段 5 已完成验收

## 2. 功能验收项（必须满足）

| 编号 | 验收项 | 通过标准（Pass 条件） | 证据 |
| --- | --- | --- | --- |
| A5-01 | 报告输入口径统一 | API/Web 执行记录可映射到统一报告输入结构，无冲突字段 | `docs/project/stage-5-reporting-input-contract.md` + 映射实现与测试 |
| A5-02 | 报告中心最小闭环可用 | 可按项目/时间范围查询报告摘要（总量、通过率、失败率、TopN） | 报告接口与前端页面 |
| A5-03 | 趋势分析最小闭环可用 | 支持日/周趋势查询与展示，结果可复算一致 | 趋势接口、趋势图页面、测试用例 |
| A5-04 | 失败治理可追溯 | 失败记录可分类、可筛选、可回溯原执行记录 | 失败治理接口/页面 |
| A5-05 | 权限与审计对齐 | 越权访问拒绝一致（403），关键动作有审计留痕 | 权限测试 + 审计测试 |
| A5-06 | 文档口径一致 | 阶段 5 清单、项目进度、模块 SKILL、架构文档同步一致 | `docs/project/*`、`docs/modules/future/*`、`docs/architecture/*` |

## 3. 质量门禁（必须通过）

| 门禁项 | 目标命令 | 通过标准 |
| --- | --- | --- |
| 后端阶段 5 最小回归 | `.\.venv\Scripts\python -m pytest <阶段5相关测试集>` | 全部通过 |
| 后端全量回归 | `.\.venv\Scripts\python -m pytest` | 全部通过（允许非阻塞 warning） |
| 前端构建 | `npm run build`（frontend） | 构建成功 |
| 迁移链路（建议） | `alembic upgrade head -> downgrade <阶段5前一revision> -> upgrade head` | 升降级成功 |

## 4. 非功能验收口径（阶段 5 出口）

- 可观测性：报告查询与关键治理动作具备审计与必要日志
- 可维护性：报告字段口径、聚合规则、页面展示三者一致
- 可恢复性：中断后可依据 S5 看板与风险清单继续推进
- 可扩展性：为阶段 6 企业集成保留稳定接口与数据模型边界

## 5. 阶段 5 验收结论口径（通过条件）

满足以下条件可判定阶段 5 验收通过：
1. A5-01~A5-06 全部满足。
2. 质量门禁中的后端最小回归、后端全量回归、前端构建全部通过。
3. 阶段文档、项目进度与架构文档已同步，不存在“占位能力标记为完成”的表述。
4. 阶段 5 风险与阻塞清单中无未声明的阻塞项。

## 6. 验收执行记录（S5-07）

- 执行日期：2026-03-17
- 后端全量回归：`.\.venv\Scripts\python -m pytest` -> `111 passed, 2 warnings in 29.23s`
- 前端构建：`npm run build`（frontend）-> 构建成功（Vite build passed）
- 结论：
  1. A5-01~A5-06 满足。
  2. 阶段 5 核心门禁（后端全量回归 + 前端构建）已通过。
  3. 阶段 5 验收结论为“通过”，已切换为“已完成验收”。
