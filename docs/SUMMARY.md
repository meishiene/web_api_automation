# 文档更新摘要

## 本次更新目的
基于当前仓库中的实际代码，修正文档中与实现不一致的部分，并补充预留能力说明。

## 已修正内容
- 把认证方式从泛化的“Token/JWT”表述修正为当前实际实现：`X-User-ID` 简化鉴权
- 把系统能力从“完整测试平台”收敛为当前代码已经落地的 MVP 范围
- 新增对 `schedule_tasks` 和 `run_queue` 两个模型的说明
- 更新仓库结构说明，反映 `app/models/`、`frontend/src/` 的真实文件布局
- 补充前端实际页面与路由现状
- 标注未打通接口：前端保留 `getTestResult()`，后端暂无对应明细接口

## 更新后的系统画像
- 后端：FastAPI + SQLAlchemy + SQLite + httpx
- 前端：Vue 3 + Vite + Vue Router + axios
- 已完成：注册登录、项目管理、测试用例管理、单条执行、结果入库
- 预留未完成：调度、队列、批量执行、独立报告、正式认证体系

## 建议后续文档方向
- 增补 API 接口文档（按请求/响应示例组织）
- 增补数据库表结构文档
- 在调度与队列功能落地后补“任务系统设计文档”
